# Bug修复报告 - 技能冷却系统

**修复时间**: 2026-01-20
**修复文件**: `src/stores/gameStore.js`

---

## 🐛 Bug描述

### 问题现象
定海神针技能使用后，显示"剩余1回合"冷却，但进入下一回合后仍然显示"剩余1回合"，导致技能永远无法再次使用。

**用户报告截图显示**:
- 回合4：123使用了定海神针，10回合内不会被交换
- 回合4结束后，技能显示"剩余1回合"
- 回合5开始，技能仍然显示"剩余1回合"（应该显示冷却完成）

### 影响范围
所有使用`cooldowns`系统的技能都受影响：
1. **定海神针** - 冷却1回合
2. **先声夺人** - 冷却3回合
3. **永久摧毁** - 冷却5回合
4. **坚不可摧** - 冷却5回合
5. **城市试炼** - 冷却5回合
6. **寸步难行** - 冷却5回合（战斗技能）

---

## 🔍 根本原因

### 问题分析

**技能冷却设置逻辑**（正常）:
```javascript
// 在nonBattleSkills.js中，技能使用时设置冷却
gameStore.cooldowns[caster.name]['定海神针'] = 1
gameStore.cooldowns[caster.name]['先声夺人'] = 3
gameStore.cooldowns[caster.name]['永久摧毁'] = 5
gameStore.cooldowns[caster.name]['坚不可摧'] = 5
gameStore.cooldowns[caster.name]['城市试炼'] = 5

// 在battleSkills.js中，战斗技能使用时设置冷却
gameStore.cooldowns[caster.name]['寸步难行'] = 5
```

**冷却递减逻辑**（缺失）:
在`gameStore.js`的`updateRoundStates()`函数中，有以下回合状态更新：
- ✅ 金币贷款冷却递减（`goldLoanRounds[playerName]--`）
- ✅ 禁用城市递减（`bannedCities[player][cityIdx]--`）
- ✅ 保护罩递减（`protections[player][cityIdx]--`）
- ✅ 伪装回合递减（`disguise.roundsLeft--`）
- ✅ 护盾回合递减（`barrier[player].roundsLeft--`）
- ✅ 坚不可摧护盾递减（`jianbukecui[player].roundsLeft--`）
- ✅ 定时炸弹递减（`timeBombs[player][cityIdx]--`）
- ✅ 天灾人祸递减（`disaster[player][cityIdx]--`）
- ✅ 金融危机递减（`financialCrisis.value.roundsLeft--`）
- ✅ 技能保护递减（`skillProtection[player].roundsLeft--`）
- ✅ 治疗状态递减（`healing.roundsLeft--`）
- ✅ 偷取技能递减（`skill.roundsLeft--`）
- ✅ 不露踪迹递减（`stealth[player].roundsLeft--`）
- ✅ 禁用技能递减（`bannedSkills[opponent][skillName]--`）
- ✅ 寸步难行递减（`stareDown[player].roundsLeft--`）
- ✅ 电磁感应递减（`link.roundsLeft--`）
- ❌ **技能冷却递减（`cooldowns`）- 缺失！**

### 为什么会遗漏？

`cooldowns`系统是后来添加的，用于实现特定技能的冷却机制，但在添加时忘记在`updateRoundStates()`中添加递减逻辑。

---

## ✅ 修复方案

### 修复代码

**文件**: `src/stores/gameStore.js`
**位置**: `updateRoundStates()`函数，line 1188-1197之后

**添加的代码**:
```javascript
// 更新技能冷却时间（定海神针、先声夺人、永久摧毁、坚不可摧、城市试炼）
for (const player of Object.keys(cooldowns)) {
  for (const skillName of Object.keys(cooldowns[player])) {
    if (cooldowns[player][skillName] > 0) {
      cooldowns[player][skillName]--
      if (cooldowns[player][skillName] <= 0) {
        delete cooldowns[player][skillName]
        addLog(`${player}的${skillName}技能冷却完成`)
      }
    }
  }
  // 如果该玩家所有技能都冷却完成了，删除该玩家条目
  if (Object.keys(cooldowns[player]).length === 0) {
    delete cooldowns[player]
  }
}
```

### 修复逻辑说明

1. **遍历所有玩家的冷却技能**
   - 外层循环：遍历`cooldowns`对象的所有玩家
   - 内层循环：遍历每个玩家的所有冷却技能

2. **递减冷却回合数**
   - 如果`cooldowns[player][skillName] > 0`，则递减1
   - 如果递减后`<= 0`，删除该技能的冷却记录
   - 添加日志通知玩家技能冷却完成

3. **清理空对象**
   - 如果玩家所有技能都冷却完成，删除该玩家的冷却记录
   - 保持数据结构整洁

---

## 🧪 测试验证

### 测试场景1：定海神针（冷却1回合）

**步骤**:
1. 回合1：玩家123使用定海神针，保护苏州市
2. 检查：技能显示"剩余1回合"
3. 回合2开始：调用`updateRoundStates()`
4. 检查：技能冷却完成，可以再次使用

**预期结果**:
- 回合1使用后：`cooldowns['123']['定海神针'] = 1`
- 回合2开始后：`cooldowns['123']['定海神针'] = 0`（被删除）
- 日志显示："123的定海神针技能冷却完成"
- 技能选择器中不再显示"剩余X回合"

### 测试场景2：先声夺人（冷却3回合）

**步骤**:
1. 回合1：玩家123使用先声夺人
2. 检查：技能显示"剩余3回合"
3. 回合2开始：冷却递减为2
4. 回合3开始：冷却递减为1
5. 回合4开始：冷却递减为0，技能可用

**预期结果**:
- 回合1使用后：`cooldowns['123']['先声夺人'] = 3`
- 回合2开始后：`cooldowns['123']['先声夺人'] = 2`
- 回合3开始后：`cooldowns['123']['先声夺人'] = 1`
- 回合4开始后：`cooldowns['123']['先声夺人'] = 0`（被删除）
- 日志显示："123的先声夺人技能冷却完成"

### 测试场景3：永久摧毁（冷却5回合）

**步骤**:
1. 回合1：玩家123使用永久摧毁
2. 检查：技能显示"剩余5回合"
3. 回合2-5：每回合冷却递减1
4. 回合6开始：冷却完成，技能可用

**预期结果**:
- 回合1使用后：`cooldowns['123']['永久摧毁'] = 5`
- 回合2-5：冷却逐步递减（5→4→3→2→1）
- 回合6开始后：`cooldowns['123']['永久摧毁'] = 0`（被删除）
- 日志显示："123的永久摧毁技能冷却完成"

### 测试场景4：多个技能同时冷却

**步骤**:
1. 回合1：玩家123使用定海神针（冷却1回合）
2. 回合2：玩家123使用先声���人（冷却3回合）
3. 回合3开始：定海神针冷却完成，先声夺人剩余2回合
4. 回合4开始：先声夺人剩余1回合
5. 回合5开始：先声夺人冷却完成

**预期结果**:
- 回合2结束：`cooldowns['123'] = {'定海神针': 1, '先声夺人': 3}`
- 回合3开始：`cooldowns['123'] = {'先声夺人': 2}`（定海神针已删除）
- 回合4开始：`cooldowns['123'] = {'先声夺人': 1}`
- 回合5开始：`cooldowns['123']`被完全删除（所有技能冷却完成）

---

## 📝 受影响的技能详情

### 1. 定海神针
- **成本**: 1金币
- **效果**: 城市10回合不会被交换
- **冷却**: 1回合
- **代码位置**: `nonBattleSkills.js:1363-1420`

### 2. 先声夺人
- **成本**: 1金币
- **效果**: 与对手交换城市
- **冷却**: 3回合
- **代码位置**: `nonBattleSkills.js:975-1050`

### 3. 永久摧毁
- **成本**: 10金币
- **效果**: 永久摧毁对手一座城市
- **冷却**: 5回合
- **代码位置**: `nonBattleSkills.js:3000-3070`

### 4. 坚不可摧
- **成本**: 5金币
- **效果**: 获得5回合护盾，免疫所有伤害
- **冷却**: 5回合
- **代码位置**: `nonBattleSkills.js:3740-3810`

### 5. 城市试炼
- **成本**: 3金币
- **效果**: 城市进入试炼状态
- **冷却**: 5回合
- **代码位置**: `nonBattleSkills.js:4360-4430`

### 6. 寸步难行
- **成本**: 5金币
- **效果**: 限制对手5回合内只能使用当机立断和无懈可击
- **冷却**: 5回合
- **代码位置**: `battleSkills.js:1577`

---

## 🎯 修复效果

### 修复前
- ❌ 技能使用后冷却时间不递减
- ❌ 技能永远显示"剩余X回合"
- ❌ 技能无法再次使用
- ❌ 玩家体验极差

### 修复后
- ✅ 技能冷却时间每回合正确递减
- ✅ 冷却完成后自动解锁
- ✅ 日志提示玩家技能可用
- ✅ 技能可以正常重复使用
- ✅ 玩家体验良好

---

## 🔄 相关系统

### 冷却系统架构

```javascript
// 数据结构
cooldowns = {
  '玩家名': {
    '技能名': 剩余回合数,
    '技能名': 剩余回合数,
    ...
  },
  ...
}

// 设置冷却（技能使用时）
gameStore.cooldowns[caster.name]['技能名'] = 冷却回合数

// 检查冷却（技能使用前）
if (gameStore.cooldowns[caster.name]?.['技能名'] > 0) {
  return { success: false, message: '技能冷却中' }
}

// 递减冷却（每回合开始时）
for (const player of Object.keys(cooldowns)) {
  for (const skillName of Object.keys(cooldowns[player])) {
    if (cooldowns[player][skillName] > 0) {
      cooldowns[player][skillName]--
      if (cooldowns[player][skillName] <= 0) {
        delete cooldowns[player][skillName]
      }
    }
  }
}
```

### 与其他系统的对比

| 系统 | 数据结构 | 递减位置 | 状态 |
|------|---------|---------|------|
| 金币贷款 | `goldLoanRounds[player]` | `updateRoundStates()` | ✅ 正常 |
| 禁用城市 | `bannedCities[player][cityIdx]` | `updateRoundStates()` | ✅ 正常 |
| 保护罩 | `protections[player][cityIdx]` | `updateRoundStates()` | ✅ 正常 |
| 技能冷却 | `cooldowns[player][skillName]` | `updateRoundStates()` | ✅ 已修复 |

---

## 📋 后续建议

### 1. 添加单元测试
为冷却系统添加专门的单元测试，确保：
- 冷却时间正确递减
- 冷却完成后正确删除
- 多个技能同时冷却时互不干扰

### 2. 统一冷却机制
考虑将所有冷却相关的逻辑统一到一个模块中：
```javascript
// cooldownManager.js
export function setCooldown(player, skill, rounds)
export function getCooldown(player, skill)
export function updateCooldowns()
export function clearCooldown(player, skill)
```

### 3. 添加UI提示
在技能选择器中更清晰地显示冷却状态：
- 冷却中：显示剩余回合数
- 冷却完成：显示"可用"或高亮显示
- 添加冷却进度条

### 4. 文档更新
更新技能文档，明确标注哪些技能有冷却时间。

---

## ✅ 验证清单

- [x] 修复代码已添加到`updateRoundStates()`
- [x] 所有5个冷却技能都会被正确处理
- [x] 冷却完成后会添加日志提示
- [x] 空的冷却记录会被清理
- [x] 修复报告已创建

---

**修复完成时间**: 2026-01-20
**修复验证**: 待测试
**影响技能数**: 6个
**修复代码行数**: 17行

---

## 🎉 总结

这是一个典型的"遗漏递减逻辑"Bug。`cooldowns`系统在添加时，只实现了设置和检查逻辑，但忘记在回合更新时添加递减逻辑。修复后，所有使用冷却机制的6个技能（定海神针、先声夺人、永久摧毁、坚不可摧、城市试炼、寸步难行）都能正常工作，玩家可以在冷却完成后重复使用这些技能。
